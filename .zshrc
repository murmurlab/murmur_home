bash ~/.murmurbox/loader.bash
#		curl -o /home/ahmbasar/._kagebunshinnojutsu https://raw.githubusercontent.com/murmurlab/OpenWAR/refs/heads/main/IMG_0581.jpg
#		amixer set Master unmute
#		amixer set Master 10%
#		gsettings set org.gnome.desktop.background picture-uri /home/ahmbasar/._kagebunshinnojutsu
#		gsettings set org.gnome.desktop.background picture-uri-dark /home/ahmbasar/._kagebunshinnojutsu
#		nohup nvlc ~/libft.h &

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

source ~/.murmurbox/loader.bash
. "$HOME/.local/bin/env"
